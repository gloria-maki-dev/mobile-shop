# Mobile Shop

Web application built with React and Vite that displays a list of mobile products, including search functionality and product detail navigation.

## Project Structure

- `src/pages` — Pages like HomePage and ProductDetailPage
- `src/components` — Reusable components like Header
- `src/services` — Functions to consume APIs

## How to run locally

```bash
npm install
npm run dev
